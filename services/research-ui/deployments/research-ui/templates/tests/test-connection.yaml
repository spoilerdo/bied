apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "research-ui.fullname" . }}-test-connection"
  labels:
    {{- include "research-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "research-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
