apiVersion: v1
kind: Service
metadata:
  namespace: nugetregistry
  name: nugetregistry-service
spec:
  type: ClusterIP
  selector:
    component: nugetregistry
  ports:
    - port: 8080
      targetPort: 80
